# Stores

État temporaire côté client.

Stores possibles :

```txt
atelierStore
uiStore
sessionStore
```

Ne pas y mettre la source de vérité persistante : celle-ci doit être en SQLite ou côté serveur.
